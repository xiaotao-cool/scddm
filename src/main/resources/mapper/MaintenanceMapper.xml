<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scddm.mapper.MaintenanceMapper">

    <insert id="insert">
        insert into device_maintenance
            (deviceId, reason, maintainer, cost, status, startTime)
        values
            (#{deviceId}, #{reason}, #{maintainer}, #{cost},
             #{status}, #{startTime})
    </insert>

    <select id="findUnfinished" resultType="com.scddm.entity.Maintenance">
        select * from device_maintenance
        where status != '已完成'
    </select>

    <select id="listAll" resultType="com.scddm.entity.Maintenance">
        select * from device_maintenance order by startTime desc
    </select>

</mapper>
